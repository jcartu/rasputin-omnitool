# Become Manus Buildout Handoff — 2026-04-28

## Summary

I started the unattended buildout and completed the first concrete integration layer:

- Created a tested local `become_manus` harness package in `/home/josh/workspace`.
- Configured and verified Playwright MCP for Hermes.
- Created a reusable `become-manus` Hermes skill.
- Generated a candidate matrix, smoke report, and demo deliverable bundle.
- Started a background Hermes continuation job to keep working unattended on license review and the next integration tasks.

## Files added in workspace

- `/home/josh/workspace/become_manus/__init__.py`
- `/home/josh/workspace/become_manus/__main__.py`
- `/home/josh/workspace/become_manus/capability_matrix.py`
- `/home/josh/workspace/become_manus/smoke.py`
- `/home/josh/workspace/become_manus/deliverables.py`
- `/home/josh/workspace/tests/test_become_manus_toolkit.py`
- `/home/josh/workspace/.hermes/prompts/become-manus-unattended-continuation.md`
- `/home/josh/workspace/outputs/become-manus/candidate-matrix.json`
- `/home/josh/workspace/outputs/become-manus/smoke/become-manus-smoke.json`
- `/home/josh/workspace/outputs/become-manus/demo/*`

## Hermes config changes

Backed up config before edit:

- `/home/josh/.hermes/backups/become-manus-20260428T193858Z/config.yaml`

Added Playwright MCP server to `~/.hermes/config.yaml`:

```yaml
mcp_servers:
  playwright:
    command: npx
    args:
      - -y
      - '@playwright/mcp@latest'
      - --headless
    timeout: 120
    connect_timeout: 60
```

Verification passed:

```bash
hermes mcp test playwright
```

Result: connected and discovered 22 tools.

## Skill created

Created Hermes skill:

- `/home/josh/.hermes/skills/autonomous-ai-agents/become-manus/SKILL.md`

Purpose: reusable instructions and commands for Manus-parity build/test work.

## Test results

Command:

```bash
cd /home/josh/workspace
python -m pytest tests/test_become_manus_toolkit.py -q
```

Result:

```text
4 passed
```

## Smoke result

Command:

```bash
python -m become_manus smoke --output-dir outputs/become-manus/smoke
```

Result: `pass`

Checks passed:

- Python available
- Node available
- npm available
- Hermes available
- Docker available
- Playwright MCP configured
- Playwright MCP connectivity and tool discovery

## Demo deliverables generated and validated

Command:

```bash
python -m become_manus demo --output-dir outputs/become-manus/demo
```

Generated:

- `source_data.csv`
- `analysis.md`
- `capability_chart.png`
- `executive_summary.html`
- `executive_summary.pdf`
- `analysis.xlsx`
- `presentation.pptx`
- `manifest.json`

Validation result: `pass`

## Candidate matrix

Command:

```bash
python -m become_manus matrix --output outputs/become-manus/candidate-matrix.json
```

Matrix currently tracks:

- 16 Manus-parity capability buckets
- 54 OSS/tool candidates
- Preferred integration stack including Playwright MCP, browser-use, agent-infra/sandbox, microsandbox, OpenHands, aider, bolt.diy, Crawl4AI, Docling, Activepieces, Puck, Supabase, Coolify, Marp, PptxGenJS, DuckDB/Jupyter/Evidence, and others.

## Background continuation job

Started background process:

- Session ID: `proc_480e6f8db661`
- Purpose: continue unattended with license review, next tests/code, sandbox/browser follow-up, and write `/home/josh/workspace/outputs/become-manus/unattended-continuation-report.md`.

Poll with:

```bash
# via Hermes process tool if in-session
process(action="poll", session_id="proc_480e6f8db661")
```

or inspect process/logs via terminal if needed.

## Important caveats

- The main Hermes repo `/home/josh/.hermes/hermes-agent` already had unrelated dirty changes in Hindsight files before this work. I did not modify those files.
- This pass builds the harness/configuration and verifies first E2E artifacts; it does not claim full Manus parity yet.
- The checklist rule remains: no Manus-parity feature should be marked complete until its E2E test passes and writes evidence under `outputs/become-manus/`.
